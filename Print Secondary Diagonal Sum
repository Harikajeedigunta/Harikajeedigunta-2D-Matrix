C++ Code:
#include<iostream>
#include<vector>
using namespace std;
int printSecondaryDiagonalSum(vector<vector<int>>& nums)
{
    int rows=nums.size();
    int cols=nums[0].size();
    int d2_sum=0;
    int limit=min(rows,cols);
    for(int i=0;i<limit;i++)
    {
        d2_sum+=nums[i][cols-i-1];
    }
    return d2_sum;
}
int main()
{
    int rows,cols;
    cin>>rows>>cols;
    vector<vector<int>>nums(rows,vector<int>(cols));
    for(int i=0;i<rows;i++)
    {
        for(int j=0;j<cols;j++)
        {
            cin>>nums[i][j];
        }
    }
    cout<<printSecondaryDiagonalSum(nums);
}

Input:
3 3
1 2 3 4 5 6 7 8 9

Output:
15
